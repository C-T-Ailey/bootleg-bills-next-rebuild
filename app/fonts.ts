import { Inter, Bungee, Bungee_Hairline, Permanent_Marker } from 'next/font/google'

export const inter = Inter({ 
    subsets: ["latin"],
    display: 'swap', 
});

export const bungee = Bungee({ 
  weight: '400',
  subsets: ["latin"],
  display: 'swap', 
});

export const hairline = Bungee_Hairline({ 
    weight: '400',
    subsets: ["latin"],
    display: 'swap', 
  });

export const marker = Permanent_Marker({ 
  weight: '400',
  subsets: ["latin"],
  display: 'swap', 
});